# Web dashboard API configuration
# Copy this file to web/.env and fill in real values

# Backend API base URL
VITE_API_BASE_URL=/api

# API auth token (must match backend AIPULSE_API_TOKEN when auth is enabled)
VITE_API_TOKEN=
